apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: dev
  namespace: kargo
spec:
  requestedFreight:
  - origin:
      kind: Warehouse
      name: demoapp-warehouse
  promotionTemplate:
    spec:
      steps:
      - uses: git-clone
        config:
          repoURL: https://github.com/rajaramiotfal/multi-cluster-gitops.git
          checkout:
            branch: main
      - uses: kustomize-set-image
        config:
          path: envs/dev
          images:
          - name: docker.io/rajarammurali513/demoapp
      - uses: git-commit
        config:
          message: "Promote image to Dev"
      - uses: git-push
